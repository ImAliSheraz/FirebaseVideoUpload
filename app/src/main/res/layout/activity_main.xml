<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <ImageView
        android:layout_width="match_parent"
        android:id="@+id/logoIV"
        android:layout_height="120dp"
        android:src="@drawable/ic_uploder"
        android:layout_marginTop="20dp"
        android:layout_alignParentTop="true" />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="20dp"
        android:layout_alignBottom="@id/logoIV"
        android:text="Upload Your Favorite Videos"
        android:gravity="center"
        android:textColor="@color/colorRed"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="10dp"
        android:text="Upload Your Video"
        android:id="@+id/moveToUploadActBtn"
        android:layout_centerInParent="true"
        android:background="@drawable/btn_background"
        android:layout_marginBottom="10dp"
        android:onClick="moveToUploadVideo"
        />

    <Button
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="10dp"
        android:text="Download Video"
        android:id="@+id/moveToDownloadActBtn"
        android:layout_below="@id/moveToUploadActBtn"
        android:background="@drawable/btn_background"
        android:onClick="moveToDownloadVideo"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="20dp"
        android:layout_alignParentBottom="true"
        android:text="Develope By Ali Sheraz"
        android:gravity="center"
        android:layout_marginBottom="20dp"
        android:textColor="@color/btnColor"/>



</RelativeLayout>
